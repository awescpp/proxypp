# Test TLS Certificates

These certificates are self-signed and are used only by proxy++ e2e tests.

The private key in this directory is intentionally committed because it is
only used for local test HTTPS servers. Do not use these certificates or keys
for production, development deployments, or any real service.